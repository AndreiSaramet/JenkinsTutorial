/* Requires the Docker Pipeline plugin */
pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'pwd'
                sh 'whoami'
                sh 'gcc'
                sh 'whereis python3.12'
            }
        }
    }
}
